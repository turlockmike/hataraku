/**
 * Gets the role section of the system prompt
 * @param role The role definition
 * @returns The role section content
 */
export function getRoleSection(role: string = 'You are a helpful assistant.'): string {
  return role
}
